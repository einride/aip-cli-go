version: v1

managed:
  enabled: true
  go_package_prefix:
    default: go.einride.tech/aip-cli/cmd/examplectl/gen
    except:
      - buf.build/googleapis/googleapis

plugins:
  # The CLI generator requires the stubs generated by protoc-gen-go.
  - name: go
    out: cmd/examplectl/gen
    opt: module=go.einride.tech/aip-cli/cmd/examplectl/gen

  # The CLI generator optionally generates a root command and a main file
  # to the root of the output module.
  - name: go-aip-cli
    out: cmd/examplectl/gen
    strategy: all
    opt:
      - module=go.einride.tech/aip-cli/cmd/examplectl/gen
      - root=examplectl
      - gcloud_identity_tokens=true
